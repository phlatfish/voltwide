{
  "name": "voltwide-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf dist/atom-builder && mkdir -p dist/atom-builder && cp -r public/atom-builder/* dist/atom-builder/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.23.1",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "svelte-spa-router": "^4.0.1"
  }
}

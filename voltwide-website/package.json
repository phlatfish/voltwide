{
  "name": "voltwide-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/atom-builder && cp -r public/atom-builder/* dist/atom-builder/ && chmod -R 755 dist/atom-builder",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.23.1",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "svelte-spa-router": "^4.0.1"
  }
}
